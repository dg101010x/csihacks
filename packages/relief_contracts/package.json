{
  "name": "@relief/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Versioned contract package shared between Plan One (ReliefFM) and Plan Two (platform). No application logic or model code.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@relief/typescript-config": "workspace:*",
    "@types/node": "^22.9.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
