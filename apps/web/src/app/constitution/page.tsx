import { RouteStub } from "@/components/route-stub";

export default function ConstitutionPage() {
  return (
    <RouteStub
      phase="Route: /constitution — consumer rules (Section 17)"
      title="Your constitution"
      description="Free-text rule capture, structured parse preview, and explicit consumer confirmation before it becomes active (Section 71)."
    />
  );
}
