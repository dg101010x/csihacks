import { RouteStub } from "@/components/route-stub";

export default function DemoPage() {
  return (
    <RouteStub
      phase="Route: /demo — guided shock demonstration (Section 17, 16.3)"
      title="Shock simulator"
      description="Baseline state, paycheck shock control, recalculated timeline, obligation collision, intervention optimizer, approval, and revised trajectory — the three-minute demonstration story (Section 20, Pass Twenty)."
    />
  );
}
