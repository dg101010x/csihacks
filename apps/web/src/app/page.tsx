import { RouteStub } from "@/components/route-stub";

export default function LandingPage() {
  return (
    <RouteStub
      phase="Route: / — landing page (Section 17)"
      title="Relief"
      description="Financial infrastructure that shows what changed, when a problem occurs, why, which interventions are available, and what happens after each one — within ten seconds (Section 15)."
    />
  );
}
