import { RouteStub } from "@/components/route-stub";

export default function OnboardingPage() {
  return (
    <RouteStub
      phase="Route: /onboarding — data source and consent flow (Section 17)"
      title="Connect your accounts"
      description="Data source selection (synthetic Wells Fargo or Plaid Sandbox) and consent capture (Section 82, item 7: record consent version)."
    />
  );
}
