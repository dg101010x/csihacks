import { RouteStub } from "@/components/route-stub";

export default function DashboardPage() {
  return (
    <RouteStub
      phase="Route: /dashboard — household financial state (Section 17, Phase B)"
      title="Dashboard"
      description="BalanceSummary, ResilienceScoreCard, IncomeForecastCard, UpcomingObligationsCard, RiskSummaryCard, DataFreshnessCard (Section 21.2)."
    />
  );
}
