import { RouteStub } from "@/components/route-stub";

export default function TimelinePage() {
  return (
    <RouteStub
      phase="Route: /timeline — detailed financial timeline (Section 17, Phase B)"
      title="Timeline"
      description="CashFlowTimeline with 7/14/30 day views, baseline and modified forecast, uncertainty band, risk region, and an alternate table representation (Section 21.3, 27)."
    />
  );
}
